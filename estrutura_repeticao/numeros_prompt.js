const numeroInicial = parseInt(prompt("Qual número inicial?"))
const numeroFinal = parseInt(prompt("Qual número final?"))

for (let i = numeroInicial; i <= numeroFinal; i++) {
    console.log(`Número: ${i}`)
}